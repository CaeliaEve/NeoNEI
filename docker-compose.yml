services:
  web:
    build: .
    ports:
      - "127.0.0.1:3002:3002"
    environment:
      NEONEI_HOST: 0.0.0.0
      NEONEI_CATALOG_ROOT: /data/catalog
    volumes:
      - "${NEONEI_CATALOG_ROOT:-./data/catalog}:/data/catalog:ro"
    restart: unless-stopped
