{
  "name": "neonei",
  "private": true,
  "workspaces": ["catalog", "backend", "frontend"],
  "engines": { "node": ">=24" },
  "scripts": {
    "build": "npm --prefix catalog run build && npm --prefix backend run build && npm --prefix frontend run build",
    "start": "npm --prefix backend start",
    "check": "npm --prefix catalog run build && npm --prefix backend run check && npm --prefix frontend run check",
    "test": "npm --prefix catalog run build && npm --prefix backend test && npm --prefix frontend test",
    "e2e": "npm --prefix frontend run e2e",
    "release": "node scripts/release.mjs"
  }
}
